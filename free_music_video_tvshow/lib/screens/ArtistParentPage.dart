import 'package:flutter/material.dart';

class ArtistParentPage extends StatelessWidget {
  const ArtistParentPage({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.red);
  }
}
