import 'package:flutter/material.dart';

class CountryParentPage extends StatelessWidget {
  const CountryParentPage({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.yellow);
  }
}