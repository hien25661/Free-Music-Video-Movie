import 'package:flutter/material.dart';

class TopListParentPage extends StatelessWidget {
  const TopListParentPage({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.black38);
  }
}
