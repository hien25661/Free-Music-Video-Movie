import 'package:flutter/material.dart';

class TrendingParentPage extends StatelessWidget {
  const TrendingParentPage({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.lightGreen);
  }
}